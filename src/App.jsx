import React, { useEffect, useMemo, useState } from 'react';
import { Menu, X, Trash2, Calculator, BarChart3, FolderPlus, PlusCircle, History, CheckCircle2, LogOut, UserPlus, Copy, MessageCircle, Send, Mail, TrendingUp, TrendingDown, MinusCircle, RefreshCw, Clock, Bell, Megaphone } from 'lucide-react';
import { supabase } from './lib/supabaseClient';
import { enablePushForUser, sendSystemPushNotification, isPushSupported, getPushPermission } from './pushNotifications';
const num = v => Number(v || 0), round05 = v => Math.round(num(v) * 20) / 20, inr = v => `₹${round05(v).toLocaleString('en-IN', { minimumFractionDigits: round05(v) % 1 ? 2 : 0, maximumFractionDigits: 2 })}`;
const today = () => { const d = new Date(); return `${String(d.getDate()).padStart(2, '0')}.${String(d.getMonth() + 1).padStart(2, '0')}.${d.getFullYear()}` };
function ago(v) { if (!v) return 'not updated'; const ms = Date.now() - new Date(v).getTime(); if (ms < 6e4) return 'just now'; const m = Math.floor(ms / 6e4); if (m < 60) return `${m} min ago`; const h = Math.floor(m / 60); if (h < 24) return `${h} hour${h > 1 ? 's' : ''} ago`; const d = Math.floor(h / 24); return `${d} day${d > 1 ? 's' : ''} ago` }
function delta(r) { const d = Number((num(r.daily_rate) - num(r.previous_daily_rate)).toFixed(2)); if (d > 0) return { cls: 'up', icon: <TrendingUp size={12} />, txt: `+${inr(d)}` }; if (d < 0) return { cls: 'down', icon: <TrendingDown size={12} />, txt: `-${inr(Math.abs(d))}` }; return { cls: 'flat', icon: <MinusCircle size={12} />, txt: 'No change' } }
function Logo({ dark = false, loading = false }) { return <img src={dark ? '/asr-logo-white.png' : '/asr-logo.png'} alt="ASR Iron" className={`asr-logo ${loading ? 'asr-logo-loading' : ''}`} draggable="false" /> }
export default function App() {
    const [screen, setScreen] = useState('login'), [user, setUser] = useState(null), [fabricators, setFabricators] = useState([]), [submissions, setSubmissions] = useState([]), [items, setItems] = useState([]), [categories, setCategories] = useState([]), [rateItems, setRateItems] = useState([]), [loading, setLoading] = useState(true), [toast, setToast] = useState(''), [confirmBox, setConfirmBox] = useState(null), [sideOpen, setSideOpen] = useState(false);
    // 2) Add this state inside App():
    const [pushPermission, setPushPermission] = useState(getPushPermission());

    // 3) Add this function inside App():
    async function enablePhoneNotifications() {
        const subscription = await enablePushForUser(user, setToast);
        setPushPermission(getPushPermission());
        return subscription;
    }
    const [loginId, setLoginId] = useState(''), [loginPassword, setLoginPassword] = useState(''), [loginError, setLoginError] = useState(''), [signup, setSignup] = useState({ name: '', mobile: '', address: '', password: '' }), [signupOk, setSignupOk] = useState(false), [signupError, setSignupError] = useState('');
    const [adminTab, setAdminTab] = useState('calculator'), [fabTab, setFabTab] = useState('apply'), [categoryId, setCategoryId] = useState(''), [sizeId, setSizeId] = useState(''), [qty, setQty] = useState('0'), [cart, setCart] = useState([]), [quoteText, setQuoteText] = useState(''), [quoteEdited, setQuoteEdited] = useState(false);
    const [newSegment, setNewSegment] = useState({ name: '', rate: '', freight: '' }), [newSizeName, setNewSizeName] = useState(''), [newSizeDiff, setNewSizeDiff] = useState(''), [marketSearch, setMarketSearch] = useState(''), [sizeSearch, setSizeSearch] = useState(''), [newItem, setNewItem] = useState({ name: '', unit: 'kg', points: '' });
    const [claimItemId, setClaimItemId] = useState(''), [claimQty, setClaimQty] = useState(''), [claimOk, setClaimOk] = useState(false);
    const [notifications, setNotifications] = useState([]), [newNotification, setNewNotification] = useState({ target: 'all', fabricator_id: '', title: '', message: '' });
    const sortedCategories = useMemo(() => [...categories].sort((a, b) => String(a.name).localeCompare(String(b.name), undefined, { numeric: true, sensitivity: 'base' })), [categories]);
    const marketCategories = useMemo(() => sortedCategories.filter(c => String(c.name).toLowerCase().includes(marketSearch.toLowerCase())), [sortedCategories, marketSearch]);
    const visibleSizes = useMemo(() => { let rows = rateItems.filter(x => x.category_id === categoryId); const c = categories.find(x => x.id === categoryId); if (c?.name?.toLowerCase() === 'pipe') rows = [...rows].reverse(); else rows = [...rows].sort((a, b) => String(a.name).localeCompare(String(b.name), undefined, { numeric: true, sensitivity: 'base' })); if (sizeSearch) rows = rows.filter(x => String(x.name).toLowerCase().includes(sizeSearch.toLowerCase())); return rows }, [rateItems, categoryId, categories, sizeSearch]);
    const activeFabricator = useMemo(() => fabricators.find(f => f.id === user?.id) || user, [fabricators, user]);
    const myClaims = useMemo(() => submissions.filter(s => s.fabricator_id === user?.id), [submissions, user]);
    const pendingPoints = useMemo(() => myClaims.filter(s => s.status === 'Pending').reduce((sum, s) => sum + num(s.points_earned), 0), [myClaims]);
    const visibleNotifications = useMemo(() => { if (!user) return []; const role = String(user.role || '').toLowerCase(); return notifications.filter(n => n.target === 'all' || n.target === role || (role === 'fabricator' && n.target === 'fabricator') || (role === 'fabricator' && n.target === `fabricator:${user.id}`)); }, [notifications, user]);
    async function loadNotifications() { try { const local = JSON.parse(localStorage.getItem('asr_notifications') || '[]'); if (!supabase) { setNotifications(local); return; } const { data, error } = await supabase.from('notifications').select('*').order('created_at', { ascending: false }); if (error) { setNotifications(local); return; } setNotifications(data || []) } catch { setNotifications([]) } }
    async function saveNotificationLocal(payload) { const existing = JSON.parse(localStorage.getItem('asr_notifications') || '[]'); const row = { ...payload, id: `local_${Date.now()}`, created_at: new Date().toISOString() }; const updated = [row, ...existing]; localStorage.setItem('asr_notifications', JSON.stringify(updated)); setNotifications(updated); return row }
    async function loadAll() { setLoading(true); if (!supabase) { await loadNotifications(); setToast('Supabase env missing. Add .env.local and restart.'); setLoading(false); return; } const [f, s, i, c, r] = await Promise.all([supabase.from('fabricators').select('*').order('created_at'), supabase.from('submissions').select('*').order('created_at', { ascending: false }), supabase.from('incentive_items').select('*').order('created_at'), supabase.from('rate_categories').select('*').order('name'), supabase.from('rate_items').select('*').order('created_at')]); if (f.error || s.error || i.error || c.error || r.error) setToast('Database load failed. Check Supabase/RLS.'); else { setFabricators(f.data || []); setSubmissions(s.data || []); setItems(i.data || []); setCategories(c.data || []); setRateItems(r.data || []); if (!categoryId && c.data?.length) setCategoryId(c.data[0].id) } await loadNotifications(); setLoading(false) }
    useEffect(() => { loadAll(); const saved = JSON.parse(localStorage.getItem('asrLogin') || 'null'); if (saved?.auto) { setLoginId(saved.loginId || ''); setLoginPassword(saved.loginPassword || ''); setUser(saved.user); setScreen(saved.role === 'admin' ? 'admin' : saved.role === 'salesman' ? 'salesman' : 'fabricator'); setAdminTab('calculator'); setFabTab('apply') } if ('serviceWorker' in navigator) window.addEventListener('load', () => navigator.serviceWorker.register('/sw.js').catch(() => { })) }, []);
    useEffect(() => { if (!toast) return; const t = setTimeout(() => setToast(''), 1000); return () => clearTimeout(t) }, [toast]); useEffect(() => { if (!quoteEdited) setQuoteText(buildQuote()) }, [cart, quoteEdited]);
    const getCategory = id => categories.find(c => c.id === id), unitRate = (catId, diff) => { const c = getCategory(catId) || {}; return round05((num(c.daily_rate) + num(diff) + num(c.freight)) * 1.18) }, askDelete = (title, onYes) => setConfirmBox({ title, onYes }), saveLogin = (role, u) => localStorage.setItem('asrLogin', JSON.stringify({ role, user: u, loginId, loginPassword, auto: true }));
    function logout() { localStorage.removeItem('asrLogin'); setUser(null); setScreen('login'); setLoginId(''); setLoginPassword('') }
    async function handleLogin(e) { e.preventDefault(); setLoginError(''); if (loginId.trim().toLowerCase() === 'admin' && loginPassword === 'admin') { const u = { id: 'admin', name: 'Admin', role: 'admin' }; setUser(u); setScreen('admin'); setAdminTab('calculator'); saveLogin('admin', u); return } if (['sales', 'salesman'].includes(loginId.trim().toLowerCase()) && ['sales123', 'salesman'].includes(loginPassword)) { const u = { id: 'salesman', name: 'Salesman', role: 'salesman' }; setUser(u); setScreen('salesman'); saveLogin('salesman', u); return } const { data, error } = await supabase.from('fabricators').select('*').eq('mobile', loginId.trim()).maybeSingle(); if (error || !data) return setLoginError('No profile registered under this mobile number.'); if (data.password !== loginPassword) return setLoginError('Incorrect password.'); if (data.status !== 'Approved') return setLoginError(`Your profile is ${data.status}. Admin approval is required.`); const u = { ...data, role: 'fabricator' }; setUser(u); setScreen('fabricator'); setFabTab('apply'); saveLogin('fabricator', u) }
    async function handleSignup(e) { e.preventDefault(); setSignupError(''); const { error } = await supabase.from('fabricators').insert({ ...signup, total_points: 0, status: 'Pending' }); if (error) return setSignupError(error.message); setSignupOk(true); setSignup({ name: '', mobile: '', address: '', password: '' }); await loadAll() }
    async function submitClaim(e) { e.preventDefault(); const item = items.find(i => i.id === claimItemId); if (!item || !claimQty || Number(claimQty) <= 0) return; const q = Number(claimQty), points = q * num(item.points_per_unit); const payload = { fabricator_id: user.id, item_id: item.id, item_name: item.name, quantity: q, unit: item.unit, points_earned: points, status: 'Pending' }; const { data, error } = await supabase.from('submissions').insert(payload).select().single(); if (error) return setToast(error.message); setSubmissions(prev => [data, ...prev]); setClaimItemId(''); setClaimQty(''); setClaimOk(true); setToast('Claim submitted for admin approval.'); setTimeout(() => setClaimOk(false), 1800) }
    async function sendNotification(e) {
        e.preventDefault(); if (!newNotification.title.trim() || !newNotification.message.trim()) return setToast('Enter notification title and message.'); let target = newNotification.target; if (target === 'specific_fabricator') { if (!newNotification.fabricator_id) return setToast('Select a fabricator.'); target = `fabricator:${newNotification.fabricator_id}` } const payload = { target, title: newNotification.title.trim(), message: newNotification.message.trim(), created_by: user?.name || 'Admin', created_at: new Date().toISOString() }; if (!supabase) { await saveNotificationLocal(payload); setToast('Notification saved locally.'); setNewNotification({ target: 'all', fabricator_id: '', title: '', message: '' }); return } const { data, error } = await supabase.from('notifications').insert(payload).select().single(); if (error) { await saveNotificationLocal(payload); setToast('Notifications table missing. Saved locally. Run SQL setup for live notifications.') } else { setNotifications(prev => [data, ...prev]); setToast('Notification sent.') } setNewNotification({ target: 'all', fabricator_id: '', title: '', message: '' })
        try {
  await sendSystemPushNotification({
    target,
    title: newNotification.title.trim(),
    message: newNotification.message.trim()
  });
} catch (err) {
  console.warn('Push send failed', err);
}
}
    async function deleteNotification(n) { askDelete(`Delete notification ${n.title}?`, async () => { if (String(n.id || '').startsWith('local_') || !supabase) { const updated = notifications.filter(x => x.id !== n.id); localStorage.setItem('asr_notifications', JSON.stringify(updated)); setNotifications(updated); setToast('Notification deleted.'); return } const { error } = await supabase.from('notifications').delete().eq('id', n.id); if (error) return setToast(error.message); setNotifications(prev => prev.filter(x => x.id !== n.id)); setToast('Notification deleted.') }) }
    async function updateMarketRate(row, newRate, newFreight = row.freight) { const patch = { previous_daily_rate: num(row.daily_rate), daily_rate: num(newRate), freight: num(newFreight), updated_at: new Date().toISOString() }; setCategories(prev => prev.map(x => x.id === row.id ? { ...x, ...patch } : x)); const { error } = await supabase.from('rate_categories').update(patch).eq('id', row.id); if (error) { setToast(error.message); await loadAll() } }
    async function addMarket(e) { e.preventDefault(); if (!newSegment.name.trim()) return; const initial = num(newSegment.rate); const { data, error } = await supabase.from('rate_categories').insert({ name: newSegment.name.trim(), daily_rate: initial, previous_daily_rate: initial, freight: num(newSegment.freight), updated_at: new Date().toISOString() }).select().single(); if (error) return setToast(error.message); setCategories(prev => [...prev, data]); setNewSegment({ name: '', rate: '', freight: '' }) }
    function deleteSegment(row) { askDelete(`Delete ${row.name}?`, async () => { await supabase.from('rate_items').delete().eq('category_id', row.id); const { error } = await supabase.from('rate_categories').delete().eq('id', row.id); if (error) return setToast(error.message); setCategories(prev => prev.filter(x => x.id !== row.id)); setRateItems(prev => prev.filter(x => x.category_id !== row.id)); setCart(prev => prev.filter(x => x.category !== row.name)); setToast(`${row.name} deleted.`) }) }
    async function addSize(e) { e.preventDefault(); if (!newSizeName.trim() || !categoryId) return; const { data, error } = await supabase.from('rate_items').insert({ category_id: categoryId, name: newSizeName, fixed_difference: num(newSizeDiff) }).select().single(); if (error) return setToast(error.message); setRateItems(prev => [...prev, data]); setNewSizeName(''); setNewSizeDiff('') }
    function deleteSize(item) { askDelete(`Delete ${item.name}?`, async () => { const { error } = await supabase.from('rate_items').delete().eq('id', item.id); if (error) return setToast(error.message); setRateItems(prev => prev.filter(x => x.id !== item.id)); setCart([]); setToast(`${item.name} deleted.`) }) }
    async function updateSizeName(item, value) { if (!value.trim()) return; setRateItems(prev => prev.map(x => x.id === item.id ? { ...x, name: value } : x)); const { error } = await supabase.from('rate_items').update({ name: value }).eq('id', item.id); if (error) { setToast(error.message); await loadAll() } }
    async function updateDiff(item, value) { setRateItems(prev => prev.map(x => x.id === item.id ? { ...x, fixed_difference: num(value) } : x)); const { error } = await supabase.from('rate_items').update({ fixed_difference: num(value) }).eq('id', item.id); if (error) { setToast(error.message); await loadAll() } }
    function addCalc(e) { e.preventDefault(); if (!categoryId || !sizeId || qty === '' || Number(qty) < 0) return; const s = rateItems.find(x => x.id === sizeId), c = getCategory(categoryId); if (!s || !c) return; const u = unitRate(categoryId, s.fixed_difference), q = Number(qty), total = q === 0 ? u : round05(u * q); setCart([...cart, { id: `cart-${Date.now()}`, category: c.name, itemName: s.name, qty: q, unitRate: u, total }]); setQuoteEdited(false); setSizeId(''); setQty('0') }
    function deleteQuoteRow(item) { askDelete(`Delete ${item.itemName}?`, () => { setCart(cart.filter(x => x.id !== item.id)); setQuoteEdited(false); setToast(`${item.itemName} deleted.`) }) }
    async function addIncentive(e) { e.preventDefault(); if (!newItem.name || !newItem.points) return; const { data, error } = await supabase.from('incentive_items').insert({ name: newItem.name, unit: newItem.unit, points_per_unit: num(newItem.points) }).select().single(); if (error) return setToast(error.message); setItems(prev => [...prev, data]); setNewItem({ name: '', unit: 'kg', points: '' }) }
    function deleteIncentive(item) { askDelete(`Delete ${item.name}?`, async () => { await supabase.from('incentive_items').delete().eq('id', item.id); setItems(prev => prev.filter(x => x.id !== item.id)); setToast(`${item.name} deleted.`) }) }
    async function approveFab(f) { await supabase.from('fabricators').update({ status: 'Approved' }).eq('id', f.id); setFabricators(prev => prev.map(x => x.id === f.id ? { ...x, status: 'Approved' } : x)); setToast(`${f.name} approved.`) } async function rejectFab(f) { await supabase.from('fabricators').update({ status: 'Rejected' }).eq('id', f.id); setFabricators(prev => prev.map(x => x.id === f.id ? { ...x, status: 'Rejected' } : x)); setToast(`${f.name} rejected.`) } async function approveSub(s) { const { error } = await supabase.rpc('approve_submission', { p_submission_id: s.id }); if (error) return setToast(error.message); setSubmissions(prev => prev.map(x => x.id === s.id ? { ...x, status: 'Approved' } : x)); await loadAll() } async function rejectSub(s) { const { error } = await supabase.rpc('reject_submission', { p_submission_id: s.id }); if (error) return setToast(error.message); setSubmissions(prev => prev.map(x => x.id === s.id ? { ...x, status: 'Rejected' } : x)) }
    function buildQuote() { const d = today(); if (!cart.length) return `          📝 *ASR Iron* \n             *${d}*\n-------------------------------------\n\n-------------------------------------\nThankyou!`; const lines = ['          📝 *ASR Iron* ', `             *${d}*`, '-------------------------------------']; cart.forEach((i, idx) => lines.push(`${idx + 1}. *${i.category.toUpperCase()} - ${i.itemName}* : *${inr(i.total)}*`, '')); lines.push('-------------------------------------', '', 'Thankyou!'); return lines.join('\n') } const finalQuote = () => quoteText || buildQuote(); async function copyQuote() { await navigator.clipboard.writeText(finalQuote()); setToast('Quotation copied.') } function whatsapp() { window.open(`https://wa.me/?text=${encodeURIComponent(finalQuote())}`, '_blank') } function telegram() { window.open(`https://t.me/share/url?url=&text=${encodeURIComponent(finalQuote())}`, '_blank') } function emailQuote() { window.open(`mailto:?subject=${encodeURIComponent('ASR Iron Quotation')}&body=${encodeURIComponent(finalQuote())}`, '_blank') } async function nativeShare() { if (navigator.share) { try { await navigator.share({ title: 'ASR Iron Quotation', text: finalQuote() }) } catch { } } else await copyQuote() } function resetQuote() { setQuoteText(buildQuote()); setQuoteEdited(false) }
    const adminMenu = [{ id: 'calculator', label: 'Calculator', icon: <Calculator size={18} /> }, { id: 'market', label: 'Market', icon: <BarChart3 size={18} /> }, { id: 'daily', label: 'Daily Rate', icon: <FolderPlus size={18} /> }, { id: 'claims', label: 'Claims', icon: <CheckCircle2 size={18} /> }, { id: 'signups', label: 'Signups', icon: <UserPlus size={18} /> }, { id: 'items', label: 'Items', icon: <PlusCircle size={18} /> }, { id: 'notifications', label: 'Notify', icon: <Bell size={18} /> }, { id: 'history', label: 'History', icon: <History size={18} /> }], fabMenu = [{ id: 'apply', label: 'Apply', icon: <PlusCircle size={18} /> }, { id: 'history', label: 'History', icon: <History size={18} /> }], salesMenu = [{ id: 'calculator', label: 'Calculator', icon: <Calculator size={18} /> }];
    function ticker() { return <div className="market-ticker"><div className="market-strip">{sortedCategories.map((row, i) => { const d = delta(row); return <div className={`rate-chip ${i < 4 ? 'fixed' : ''}`} key={row.id}><div className="rate-name">{row.name}</div><div className="rate-value">{inr(row.daily_rate)}</div><div className={`rate-change ${d.cls}`}>{d.icon}{d.txt}</div><div className="rate-time">Updated {ago(row.updated_at || row.created_at)}</div></div> })}</div></div> }
    function marketPage() { return <div className="grid"><div className="area"><h2 className="section-title"><BarChart3 size={20} /> Daily Market Rates</h2><p className="section-note">Increment/decrement compares current value with previous saved update and remains after refresh.</p><input className="input search-box" placeholder="Search market category..." value={marketSearch} onChange={e => setMarketSearch(e.target.value)} /><form className="small-card" onSubmit={addMarket}><h3>Add New Market Item</h3><div className="grid grid-3"><input className="input" placeholder="Name" value={newSegment.name} onChange={e => setNewSegment({ ...newSegment, name: e.target.value })} /><input className="input" type="number" placeholder="Daily Rate" value={newSegment.rate} onChange={e => setNewSegment({ ...newSegment, rate: e.target.value })} /><input className="input" type="number" placeholder="Freight" value={newSegment.freight} onChange={e => setNewSegment({ ...newSegment, freight: e.target.value })} /></div><button className="btn btn-primary full">Add to Market</button></form></div><div className="market-grid">{marketCategories.map(row => { const d = delta(row); return <div className="market-card" key={row.id}><div className="market-card-head"><div><div className="rate-name">{row.name}</div><div className="rate-value">{inr(row.daily_rate)}</div><div className={`rate-change ${d.cls}`}>{d.icon}{d.txt}</div><div className="rate-time">Last updated {ago(row.updated_at || row.created_at)}</div></div><button className="market-delete-btn" onClick={() => deleteSegment(row)}><Trash2 size={16} /></button></div><div className="market-update-grid"><div className="field"><label className="label">New Daily Rate</label><input className="input" type="number" defaultValue={row.daily_rate} onBlur={e => updateMarketRate(row, e.target.value, row.freight)} /></div><div className="field"><label className="label">Freight</label><input className="input" type="number" defaultValue={row.freight} onBlur={e => updateMarketRate(row, row.daily_rate, e.target.value)} /></div></div></div> })}</div></div> }
    function dailyPage() { return <div className="area"><h2 className="section-title">Daily Rate Configuration</h2><div className="field"><label className="label">Select Segment</label><select value={categoryId} onChange={e => { setCategoryId(e.target.value); setSizeSearch('') }}>{sortedCategories.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select></div><input className="input search-box" placeholder="Search size/name..." value={sizeSearch} onChange={e => setSizeSearch(e.target.value)} /><form className="small-card" onSubmit={addSize}><div className="grid grid-3"><input className="input" value={newSizeName} onChange={e => setNewSizeName(e.target.value)} placeholder="Size name" /><input className="input" type="number" value={newSizeDiff} onChange={e => setNewSizeDiff(e.target.value)} placeholder="Diff" /><button className="btn btn-primary">Add Size</button></div></form><div className="size-card-list">{visibleSizes.map(s => <div className="size-edit-card" key={s.id}><div className="size-edit-main"><input className="input size-name-input" defaultValue={s.name} onBlur={e => updateSizeName(s, e.target.value)} /><div className="size-preview">Preview: <b>{inr(unitRate(s.category_id, s.fixed_difference))}/kg</b></div></div><div className="size-edit-side"><input className="input diff-input" type="number" defaultValue={s.fixed_difference} onBlur={e => updateDiff(s, e.target.value)} /><button className="size-delete-btn" onClick={() => deleteSize(s)}><Trash2 size={16} /></button></div></div>)}</div></div> }
    function calculatorPage() { const activeSizes = rateItems.filter(x => x.category_id === categoryId), total = cart.reduce((s, i) => s + i.total, 0); return <div className="grid grid-5"><div className="area"><h3 className="section-title"><Calculator size={20} /> Generate New Estimate</h3><form onSubmit={addCalc}><div className="field"><label className="label">Step 1: Select Category</label><select value={categoryId} onChange={e => { setCategoryId(e.target.value); setSizeId(''); setQty('0') }} required><option value="">-- Choose Category --</option>{sortedCategories.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select></div>{categoryId && <div className="field"><label className="label">Step 2: Choose Size</label><select value={sizeId} onChange={e => { setSizeId(e.target.value); setQty('0') }} required><option value="">-- Select Size --</option>{activeSizes.map(s => <option key={s.id} value={s.id}>{s.name} (Diff: ₹{s.fixed_difference})</option>)}</select></div>}{categoryId && sizeId && <div className="small-card"><div className="field"><label className="label">Quantity kg</label><input className="input" type="number" min="0" step="0.1" value={qty} onChange={e => setQty(e.target.value)} required /></div></div>}<button className="btn btn-primary full" disabled={!categoryId || !sizeId || qty === '' || Number(qty) < 0}>Add Item</button></form></div><div className="area quote-summary-area"><h3 className="section-title compact-title">Calculated Quote Summary</h3>{cart.length === 0 ? <div className="empty compact-empty"><Calculator size={32} /><p>No items added.</p></div> : <><div className="quote-mini-list">{cart.map(i => <div className="quote-mini-row" key={i.id}><div><div className="quote-mini-item">{i.itemName}</div><div className="quote-mini-cat">{i.category} . {inr(i.unitRate)}{i.qty === 0 ? '' : ` * ${i.qty}`}</div></div><div className="quote-mini-price">{inr(i.total)}</div><button className="quote-mini-delete" onClick={() => deleteQuoteRow(i)}><X size={14} /></button></div>)}</div><div className="editable-quote"><label className="label">Editable quotation text</label><textarea className="input quotation-textarea" value={quoteText} onChange={e => { setQuoteText(e.target.value); setQuoteEdited(true) }} /></div><div className="share-panel compact-share"><button className="share-btn copy" onClick={copyQuote}><Copy size={15} /><span>Copy</span></button><button className="share-btn whatsapp" onClick={whatsapp}><MessageCircle size={15} /><span>WhatsApp</span></button><button className="share-btn telegram" onClick={telegram}><Send size={15} /><span>Telegram</span></button><button className="share-btn email" onClick={emailQuote}><Mail size={15} /><span>Email</span></button><button className="share-btn native" onClick={nativeShare}><Send size={15} /><span>Share</span></button><button className="share-btn reset" onClick={resetQuote}><X size={15} /><span>Reset</span></button></div><div className="quote-total compact-total"><div><div className="label">Total kg</div><b>{cart.reduce((s, i) => s + i.qty, 0).toFixed(1)} kg</b></div><div><div className="label">Estimated Invoice</div><div className="big-green">{inr(total)}</div></div></div></>}</div></div> }
    function itemsPage() { return <div className="items-page-wrap"><div className="area"><h2 className="section-title">Add Item</h2><form onSubmit={addIncentive}><div className="field"><input className="input" placeholder="Item" value={newItem.name} onChange={e => setNewItem({ ...newItem, name: e.target.value })} required /></div><div className="field"><select className="input" value={newItem.unit} onChange={e => setNewItem({ ...newItem, unit: e.target.value })}><option value="kg">kg</option><option value="pcs">pcs</option><option value="ft">ft</option></select></div><div className="field"><input className="input" type="number" placeholder="Points" value={newItem.points} onChange={e => setNewItem({ ...newItem, points: e.target.value })} required /></div><button className="btn btn-primary full">Save</button></form></div><div className="items-list-wrap">{items.map(i => <div className="incentive-item-card" key={i.id}><div className="item-name-chip">{i.name}</div><div className="item-points-chip">{i.points_per_unit} points / {i.unit}</div><button className="item-delete-strip" onClick={() => deleteIncentive(i)}><Trash2 size={18} /></button></div>)}</div></div> }
    function signupsPage() { const pending = fabricators.filter(f => f.status === 'Pending'), approved = fabricators.filter(f => f.status === 'Approved'); return <div className="grid"><div className="area"><h2 className="section-title">Pending Signups</h2>{pending.length === 0 ? <div className="empty">No pending signups.</div> : pending.map(f => <div className="signup-card" key={f.id}><div><b>{f.name}</b><p>{f.mobile} • {f.address}</p></div><div className="signup-actions"><button className="btn btn-success" onClick={() => approveFab(f)}>Approve</button><button className="btn btn-danger" onClick={() => rejectFab(f)}>Reject</button></div></div>)}</div><div className="area"><h2 className="section-title">Approved Fabricators</h2>{approved.map(f => <div className="small-card" key={f.id}><b>{f.name}</b><p>{f.mobile} • {f.total_points} pts</p></div>)}</div></div> }
    function claimsPage() { const pending = submissions.filter(s => s.status === 'Pending'); return <div className="area"><h2 className="section-title">Pending Claims</h2>{pending.length === 0 ? <div className="empty">No pending claims.</div> : pending.map(s => { const fab = fabricators.find(f => f.id === s.fabricator_id); return <div className="claim-admin-card" key={s.id}><div className="claim-admin-top"><div><div className="claim-item-name">{s.item_name}</div><div className="claim-by-line">Claim by: <b>{fab?.name || 'Unknown Fabricator'}</b>{fab?.mobile ? <span> · {fab.mobile}</span> : null}</div><p className="claim-meta">{s.quantity} {s.unit} • {s.points_earned} pts</p></div><div className="claim-points-badge">+{s.points_earned}<span>pts</span></div></div><div className="signup-actions claim-actions-row"><button className="btn btn-success" onClick={() => approveSub(s)}>Approve</button><button className="btn btn-danger" onClick={() => rejectSub(s)}>Decline</button></div></div> })}</div> }
    function historyPage() { return <div className="area"><h2 className="section-title">Processed Logs</h2>{submissions.filter(s => s.status !== 'Pending').map(s => <div className="small-card" key={s.id}><b>{s.item_name}</b><p>{s.points_earned} pts • {s.status}</p></div>)}</div> }
    function fabricatorHome() { const name = activeFabricator?.name || user?.name || 'Partner'; return <div className="fabricator-wrap"><div className="fabricator-tabs"><button className={fabTab === 'apply' ? 'active' : ''} onClick={() => setFabTab('apply')}><PlusCircle size={17} />Apply Incentive</button><button className={fabTab === 'history' ? 'active' : ''} onClick={() => setFabTab('history')}><History size={17} />History</button></div><div className="points-card"><div><span>ASR IRON APPROVED POINTS</span><div className="points-gold-box"><strong>{num(activeFabricator?.total_points).toLocaleString()}</strong><em>pts</em></div></div><div className="workshop-pill">Registered Workshop:<b>{name}</b></div></div><div className="pending-card"><div><Clock size={16} />Awaiting Review</div><span>Pending Claims Points</span><strong>{pendingPoints.toLocaleString()} <em>pts</em></strong></div>{fabTab === 'apply' ? applyIncentivePage() : fabricatorHistoryPage()}</div> }
    function applyIncentivePage() { const item = items.find(i => i.id === claimItemId); return <div className="area fab-apply-card"><h2 className="section-title"><PlusCircle size={20} />Apply for Incentive</h2>{claimOk && <div className="success">Submitted for admin approval.</div>}<form onSubmit={submitClaim}><div className="field"><label className="label">Select Fabrication Item</label><select value={claimItemId} onChange={e => { setClaimItemId(e.target.value); setClaimQty('') }} required><option value="">-- Click to Select an Item --</option>{items.map(i => <option key={i.id} value={i.id}>{i.name} ({i.points_per_unit} Pts/{i.unit})</option>)}</select></div>{item && <div className="quantity-box"><label className="label">Completed Quantity (in {item.unit})</label><div className="qty-input-wrap"><input className="input" type="number" min="0.01" step="0.01" placeholder={`Enter quantity in ${item.unit}`} value={claimQty} onChange={e => setClaimQty(e.target.value)} required /><span>{item.unit.toUpperCase()}</span></div>{claimQty && <p className="claim-preview">Points: <b>{(Number(claimQty) * num(item.points_per_unit)).toLocaleString()} pts</b></p>}</div>}<button className="btn btn-primary full submit-claim-btn">Submit for Admin Approval</button></form></div> }
    function fabricatorHistoryPage() { return <div className="area"><h2 className="section-title"><History size={20} />My Claim History</h2>{myClaims.length === 0 ? <div className="empty">No claims submitted yet.</div> : myClaims.map(s => <div className="claim-history-card" key={s.id}><div><b>{s.item_name}</b><p>{s.quantity} {s.unit} • {s.points_earned} pts</p></div><span className={`status-pill ${String(s.status).toLowerCase()}`}>{s.status}</span></div>)}</div> }
    function notificationPanel() { return <div className="notification-panel"><div className="notification-panel-head"><Bell size={16} /><span>Notifications</span></div>{visibleNotifications.slice(0, 3).map(n => <div className="notification-mini-card" key={n.id || `${n.title}-${n.created_at}`}><b>{n.title}</b><p>{n.message}</p><small>{n.created_by ? `From ${n.created_by} • ` : ''}{ago(n.created_at)}</small></div>)}</div> }
    function notificationsPage() { return <div className="grid"><div className="area notification-compose"><h2 className="section-title"><Megaphone size={20} />Send Notification</h2><form onSubmit={sendNotification}><div className="field"><label className="label">Send to</label><select className="input" value={newNotification.target} onChange={e => setNewNotification({ ...newNotification, target: e.target.value, fabricator_id: '' })}><option value="all">Everyone</option><option value="admin">Admins</option><option value="salesman">Salesman</option><option value="fabricator">All Fabricators</option><option value="specific_fabricator">Specific Fabricator</option></select></div>{newNotification.target === 'specific_fabricator' && <div className="field"><label className="label">Fabricator</label><select className="input" value={newNotification.fabricator_id} onChange={e => setNewNotification({ ...newNotification, fabricator_id: e.target.value })}><option value="">-- Select Fabricator --</option>{fabricators.filter(f => f.status === 'Approved').map(f => <option key={f.id} value={f.id}>{f.name} - {f.mobile}</option>)}</select></div>}<div className="field"><label className="label">Title</label><input className="input" placeholder="Example: New market rate update" value={newNotification.title} onChange={e => setNewNotification({ ...newNotification, title: e.target.value })} /></div><div className="field"><label className="label">Message</label><textarea className="input notification-textarea" placeholder="Write notification message..." value={newNotification.message} onChange={e => setNewNotification({ ...newNotification, message: e.target.value })} /></div><button className="btn btn-primary full">Send Notification</button></form></div><div className="area"><h2 className="section-title"><Bell size={20} />Notification History</h2>{notifications.length === 0 ? <div className="empty">No notifications sent yet.</div> : notifications.map(n => <div className="notification-history-card" key={n.id || `${n.title}-${n.created_at}`}><div><div className="notification-target-pill">{String(n.target || 'all').startsWith('fabricator:') ? 'Specific Fabricator' : n.target}</div><b>{n.title}</b><p>{n.message}</p><small>{n.created_by ? `By ${n.created_by} • ` : ''}{ago(n.created_at)}</small></div><button className="notification-delete" onClick={() => deleteNotification(n)}><Trash2 size={15} /></button></div>)}</div></div> }
    function sideMenu(type) { const isAdmin = type === 'admin', isSales = type === 'salesman', list = isSales ? salesMenu : isAdmin ? adminMenu : fabMenu, current = isAdmin ? adminTab : fabTab, setTab = isAdmin ? setAdminTab : setFabTab; return <><div className={`sidebar-backdrop ${sideOpen ? 'show' : ''}`} onClick={() => setSideOpen(false)} /><aside className={`side-menu ${sideOpen ? 'open' : ''}`}><div className="side-menu-head"><Logo dark /><button className="side-close" onClick={() => setSideOpen(false)}><X size={20} /></button></div><div className="side-menu-title">{isSales ? 'Salesman Menu' : isAdmin ? 'Admin Menu' : 'Fabricator Menu'}</div><div className="side-menu-list">{list.map(item => <button key={item.id} className={`side-menu-item ${current === item.id ? 'active' : ''}`} onClick={() => { setTab(item.id); setSideOpen(false) }}>{item.icon}<span>{item.label}</span></button>)}</div></aside></> }
    function bottomNav(type) { const isAdmin = type === 'admin', isSales = type === 'salesman', base = isSales ? salesMenu : isAdmin ? adminMenu : fabMenu, list = isAdmin ? base : [...base, { id: 'logout', label: 'Logout', icon: <LogOut size={18} /> }], current = isAdmin ? adminTab : fabTab; return <div className="bottom-nav-scroll">{list.map(item => <button key={item.id} className={current === item.id ? 'active' : ''} onClick={() => item.id === 'logout' ? logout() : isAdmin ? setAdminTab(item.id) : setFabTab(item.id)}>{item.icon}<span>{item.label}</span></button>)}</div> }
    function confirmDialog() { if (!confirmBox) return null; return <div className="confirm-backdrop"><div className="confirm-box"><button className="confirm-x" onClick={() => setConfirmBox(null)}><X size={16} /></button><div className="confirm-icon"><Trash2 size={20} /></div><h3>{confirmBox.title}</h3><p>This action cannot be undone.</p><div className="confirm-actions"><button className="btn btn-soft" onClick={() => setConfirmBox(null)}>No</button><button className="btn btn-danger" onClick={async () => { const fn = confirmBox.onYes; setConfirmBox(null); await fn() }}>Yes, delete</button></div></div></div> }
    function Header({ type }) {
        const isDark = type === 'admin';
        const alertLabel = pushPermission === 'granted' ? 'Alerts On' : pushPermission === 'denied' ? 'Alerts Blocked' : 'Enable Phone Alerts';
        return <>
            <header className={`header ${isDark ? 'dark' : ''}`}>
                <div className="head-inner">
                    <div className="brand-row">
                        <button className="hamburger-btn" onClick={() => setSideOpen(true)}><Menu size={22} /></button>
                        <Logo dark={isDark} />
                        {isDark && <span className="admin-badge">ADMIN</span>}
                        {type === 'fabricator' && <span className="partner-badge">FABRICATOR PARTNER<br /><b>{activeFabricator?.name || user?.name}</b></span>}
                    </div>

                    <div className="header-actions" style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
                        <button
                            type="button"
                            className={`btn ${pushPermission === 'granted' ? 'btn-success' : pushPermission === 'denied' ? 'btn-soft' : 'btn-gold'} push-enable-btn`}
                            onClick={enablePhoneNotifications}
                            title="Enable phone notifications"
                            style={{ minHeight: 34, padding: '8px 10px', borderRadius: 12, fontSize: 11, whiteSpace: 'nowrap' }}
                        >
                            <Bell size={15} />
                            {alertLabel}
                        </button>

                        <button className="btn btn-soft header-logout-btn" onClick={logout}>
                            <LogOut size={16} />{isDark ? ' Sign Out' : ''}
                        </button>
                    </div>
                </div>
            </header>
            {sideMenu(type)}
        </>
    }

    function phoneAlertBanner() { if (!user) return null; return <div style={{margin:'0 0 14px 0',padding:'12px 14px',border:'1px solid rgba(242,158,24,.35)',borderRadius:18,background:'linear-gradient(135deg,#fff7ed 0%,#eff6ff 100%)',boxShadow:'0 10px 24px rgba(15,23,42,.08)',display:'flex',alignItems:'center',justifyContent:'space-between',gap:12}}><div style={{display:'flex',alignItems:'center',gap:10,color:'#0A3E7A'}}><Bell size={18}/><div><b style={{display:'block',fontSize:12,fontWeight:950,color:'#0f172a'}}>Phone Notifications</b><span style={{display:'block',fontSize:10.5,fontWeight:800,color:'#64748b'}}>Tap once to enable WhatsApp-like phone alerts.</span></div></div><button type="button" className="btn btn-gold" onClick={enablePhoneNotifications}>Enable Phone Alerts</button></div> }

    function dashboard(type) { const isAdmin = type === 'admin', isSales = type === 'salesman', tab = isAdmin ? adminTab : fabTab; return <div className="screen"><Header type={type} /><main className="main">{phoneAlertBanner()}{(isAdmin ? tab !== 'notifications' : true) && visibleNotifications.length > 0 && notificationPanel()}{isSales && ticker()}{isSales && calculatorPage()}{isAdmin && ['calculator', 'market', 'daily'].includes(tab) && ticker()}{isAdmin && tab === 'calculator' && <div className="area">{calculatorPage()}</div>}{isAdmin && tab === 'market' && marketPage()}{isAdmin && tab === 'daily' && dailyPage()}{isAdmin && tab === 'claims' && claimsPage()}{isAdmin && tab === 'signups' && signupsPage()}{isAdmin && tab === 'items' && itemsPage()}{isAdmin && tab === 'notifications' && notificationsPage()}{isAdmin && tab === 'history' && historyPage()}{type === 'fabricator' && fabricatorHome()}</main>{bottomNav(type)}</div> }
    function loginPage() { return <div className="screen center"><div className="login-card card"><div className="login-head"><Logo /><div className="bar" /><p className="subtitle">Incentive Reward Portal</p></div><div className="login-body">{loginError && <div className="error">{loginError}</div>}<form onSubmit={handleLogin}><div className="field"><label className="label">Mobile / Username</label><input className="input" value={loginId} onChange={e => setLoginId(e.target.value)} required /></div><div className="field"><label className="label">Password</label><input className="input" type="password" value={loginPassword} onChange={e => setLoginPassword(e.target.value)} required /></div><button className="btn btn-primary full">Sign In</button></form><p className="muted" style={{ fontSize: 12 }}>Salesman: sales / sales123</p><div style={{ height: 20 }} /><button className="btn btn-outline full" onClick={() => { setScreen('signup'); setSignupOk(false) }}><UserPlus size={18} />Register as Partner</button></div></div></div> }
    function signupPage() { return <div className="screen center"><div className="login-card card"><div className="login-head"><Logo /><div className="bar" /><p className="subtitle">Fabricator Sign Up</p></div><div className="login-body">{signupOk ? <div><CheckCircle2 size={56} color="#059669" /><h3>Registration Complete</h3><p className="muted">Admin approval is required before login.</p><button className="btn btn-primary full" onClick={() => setScreen('login')}>Back</button></div> : <form onSubmit={handleSignup}>{signupError && <div className="error">{signupError}</div>}<input className="input" placeholder="Shop Name" value={signup.name} onChange={e => setSignup({ ...signup, name: e.target.value })} required /><br /><br /><input className="input" placeholder="Mobile" pattern="[0-9]{10}" value={signup.mobile} onChange={e => setSignup({ ...signup, mobile: e.target.value })} required /><br /><br /><textarea className="input" placeholder="Address" value={signup.address} onChange={e => setSignup({ ...signup, address: e.target.value })} required /><br /><br /><input className="input" type="password" placeholder="Password" value={signup.password} onChange={e => setSignup({ ...signup, password: e.target.value })} required /><br /><br /><button className="btn btn-primary full">Submit for Approval</button><button type="button" className="btn btn-soft full" onClick={() => setScreen('login')}>Back</button></form>}</div></div></div> }
    if (loading) return <div className="asr-loading-screen"><div className="asr-loading-card"><Logo loading /><div className="asr-loading-dots"><span /><span /><span /></div></div></div>;
    return <div>{toast && <div className="toast toast-message"><span>{toast}</span><button className="toast-close" onClick={() => setToast('')}><X size={16} /></button></div>}{confirmDialog()}{screen === 'login' && loginPage()}{screen === 'signup' && signupPage()}{screen === 'admin' && user && dashboard('admin')}{screen === 'fabricator' && user && dashboard('fabricator')}{screen === 'salesman' && user && dashboard('salesman')}</div>
}
